import { UserCard } from "./components/UserCard";
import "./styles.css";

const user = {
    id: 1,
    name: "Soh",
    email: "123@example.com",
    address: "ADDRESS"
};

export default function App() {
    const on

    return (
        <div className="App">
            <UserCard user={user} />
        </div>
    );
}
